<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle">@style/LightActionBar</item>
        <item name="windowActionBar">false</item>
    </style>
    
    <style name="LightActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextStyle">@style/LightActionBar.TitleTextStyle</item>
    </style>

    <style name="LightActionBar.TitleTextStyle" parent="Base.TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
    </style>

</resources>
